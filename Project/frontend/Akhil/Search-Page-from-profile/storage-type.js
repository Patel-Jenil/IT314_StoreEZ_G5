document.getElementById("storageForm").addEventListener("submit", function(event) {
  event.preventDefault();
  const selectedStorageType = document.getElementById("storageType").value;
  // // Perform your search using the selectedStorageType
  // // Display the results in the "searchResults" div
  // document.getElementById("searchResults").textContent = `Searching for ${selectedStorageType} storage...`;
});